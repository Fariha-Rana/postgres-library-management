import Loading from "@/components/Loading";

function LoadingOnSuspense() {
  return (
    <div className="h-screen flex justify-center items-center">
      <Loading />;
    </div>
  );
}

export default LoadingOnSuspense;
